import React from "react";
import Card from "../UI/Card";
import MainTemplate from "../UI/MainTemplate";

const PageTwo = () => {
  return (
    <MainTemplate>
      <Card>
        <h2>Welcome to PageTwo!!</h2>
      </Card>
    </MainTemplate>
  );
};

export default PageTwo;

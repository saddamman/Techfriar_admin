import React from "react";
import Card from "../UI/Card";
import MainTemplate from "../UI/MainTemplate";

const PageThree = () => {
  return (
    <MainTemplate>
      <Card>
        <h2>Welcome to PageThree!!</h2>
      </Card>
    </MainTemplate>
  );
};

export default PageThree;

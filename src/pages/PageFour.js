import React from "react";
import Card from "../UI/Card";
import MainTemplate from "../UI/MainTemplate";

const PageFour = () => {
  return (
    <MainTemplate>
      <Card>
        <h2>Welcome to PageFour!!</h2>
      </Card>
    </MainTemplate>
  );
};

export default PageFour;
